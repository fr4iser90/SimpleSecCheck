name: checkov
install:
  - ["pip3", "install", "checkov"]
  - ["pip3", "install", "--upgrade", "bc-detect-secrets"]
assets:
  - id: config
    type: config
    description: Checkov config
    mount:
      host_subpath: scanner/scanners/checkov/config/config.yaml
      container_path: /app/scanner/scanners/checkov/config/config.yaml