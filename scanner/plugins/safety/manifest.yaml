name: safety
version: "1.0"
description: Python dependency vulnerability scanner. Checks Python packages against known vulnerabilities.
categories:
  - Dependency Scanning
icon: "🐍"
category: dependency
languages:
  - python
severity_supported: true
timeout: 300
homepage: https://github.com/pyupio/safety
documentation: https://docs.pyup.io/docs/safety-cli
install:
  - ["pip3", "install", "safety"]