name: sonarqube
install:
  - ["npm", "install", "-g", "sonar-scanner"]
assets:
  - id: config
    type: config
    description: SonarQube config
    mount:
      host_subpath: scanner/scanners/sonarqube/config/config.yaml
      container_path: /app/scanner/scanners/sonarqube/config/config.yaml