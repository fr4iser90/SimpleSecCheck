name: checkov
install:
  - ["pip3", "install", "checkov"]
assets:
  - id: config
    type: config
    description: Checkov config
    mount:
      host_subpath: scanner/scanners/checkov/config/config.yaml
      container_path: /SimpleSecCheck/scanner/scanners/checkov/config/config.yaml