name: terraform
install:
  - ["pip3", "install", "checkov"]
assets:
  - id: config
    type: config
    description: Terraform Checkov config
    mount:
      host_subpath: scanner/scanners/terraform/config/config.yaml
      container_path: /SimpleSecCheck/scanner/scanners/terraform/config/config.yaml